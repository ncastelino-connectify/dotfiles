#factorial
define f(n) { 
	if (n < 0) {
		print "oops";
		halt;
	}
	if (n < 2) return 1;
		return n*f(n-1);
}

#choose probability
define c(x, y) {
	return f(x)/(f(y)*f(x-y))
}

#binomeal factorial part
define be(a,b,c,d) {
	return c(a,b)*c(c,d)
}

