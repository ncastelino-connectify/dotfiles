#!/bin/bash

repoDir='/home/neil/dotfiles/'
cd "$repoDir"

blurb=$(git pull)

echo "$blurb" | grep -i error
E="$?"
echo "$blurb" | grep -i fatal
F="$?"

if (( $E == 0 )) || (( $F ==0 )); then
	echo "$blurb"
fi
